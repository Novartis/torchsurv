repos:
    - repo: https://github.com/pre-commit/pre-commit-hooks
        rev: v4.4.0
        hooks:
            - id: check-added-large-files
                args: ['--maxkb=500']  # Set the maximum file size in kilobytes (e.g., 500 KB)